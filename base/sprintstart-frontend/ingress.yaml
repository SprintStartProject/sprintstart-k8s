apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sprintstart-frontend
spec:
  tls:
  - hosts:
    - sprintstart.local
    secretName: sprintstart-tls
  rules:
  - host: sprintstart.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: sprintstart-frontend
            port:
              number: 80
